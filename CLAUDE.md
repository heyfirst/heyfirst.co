# heyfirst.co

This repository is **public**. Do not commit secrets, API keys, credentials, or personal/private information beyond what's already published on the site.

See `CONTEXT.md` for the project's domain glossary (Post, Note, Collection, etc.). Record architectural decisions as ADRs in `docs/adr/` when one is warranted (see `docs/adr/` convention — created lazily, only when the first ADR is needed).
